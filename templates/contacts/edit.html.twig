{% extends 'layout.html.twig' %}

{% block title %}Edit Contacts{% endblock %}

{% block content %}
    <h1 class="h3 mb-2 text-gray-800">Edit Contacts
    <div class="col-6 float-right text-right">
    <div class="col-6 float-right text-right">
            <div>
                <div class="col-* ml-2 float-right">
                    {{ include('contacts/_delete_form.html.twig') }}
                </div>
                <div class="col-*">
                    <a class="btn btn-dark" href="{{ path('app_contacts_index') }}">back to list</a>
                </div>
            </div>
            
            </div>
    </h1>

     {{ include('contacts/_form.html.twig', {'button_label': 'Update'}) }}
   
{% endblock %}
